import React from "react";
import { StyleSheet, View, Text } from "react-native";

const MapMobile = () => {
	return (
		<View>
			<Text style={styles.subtitle}>Map Here haha</Text>
		</View>
	);
};

const styles = StyleSheet.create({
	subtitle: {
		fontSize: 16,
		color: "#9ca3af",
		marginTop: 8,
	},
});

export default MapMobile;
